# Performance testing

No throughput target exists in bootstrap. A later reproducible baseline will vary state size, commit count, patch size, branch count, concurrent writers, and DAG shape; report p50/p95/p99, throughput, CPU, RSS, and storage. Gate regressions against Cognitive Ledger's own accepted baseline. Fluree timing remains informational.
