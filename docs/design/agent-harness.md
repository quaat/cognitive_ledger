# Agent harness

`AGENTS.md` and `CLAUDE.md` are concise maps. Stable detail lives in this documentation, ADRs, the one active execution plan, and canonical `skills/`. Mechanical checks live in `scripts/`; provider adapters call rather than duplicate them. Reviews are bounded and evidence-seeking. The main session retains integration ownership.
